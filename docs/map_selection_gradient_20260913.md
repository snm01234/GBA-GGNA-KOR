# 맵 선택 제목 및 그라데이션 복원

2026-09-13 갱신 ss1 요청. `マップ選択` 제목은 BG2 배경 리소스 `0x08CC0E54`에 포함된 그림이다.

- 기준 TIP: `729fb5474dd98d7c8b9ecb81368049bf92e6862fd4cae0f420b584a098f7a3f3`.
- 갱신 ss1 SHA-256: `83e3019e1c6e7157871fe0d41ac21415aadaccb6b0379839a32872b52221aac7`.
- 기존 일본어 글자와 그림자가 포함된 `(6,4)-(68,19)` 영역을 전부 지웠다.
- 제목 양옆 x=4/68의 동일 높이 색이 일치함을 확인하고, 각 행 색으로 배경을 복원했다. 인덱스 순서는 `3,4,4,5,5,6,6,7,7,7,8,8,10,10,10`이다.
- Galmuri11로 `맵 선택`을 그렸다. 기존 제목의 밝은 글자·갈색 외곽선 색을 사용했다.
- 새 비압축 BG 리소스를 `0x093D4000`에 배치하고 소비자 `0x0006B6C0`, `0x0007AD14`를 갱신했다. 원본 리소스는 보존했다.
- 번역 문자열, 배터리 세이브, savestate, 다른 ROM 영역은 변경하지 않았다. 이번 변경은 배경 이미지 타일이다.

검증: 기존 배경 그래픽 및 600개 타일맵 항목이 갱신 ss1의 VRAM과 일치했다. 새 리소스 재해독 결과와 기대 픽셀이 일치하고, 제목 영역 밖 화면 픽셀은 수정 전과 동일하다. 화면 재구성을 시각적으로 확인했다. 실시간 에뮬레이터 재생 검증은 수행하지 않았다.

결과 SHA-256: `c3f145c2e6b1511643b2b832275bcfeb1587a73bc92dfbb43e7802216deff6c3`.

- 생성 도구: `tools/patch_ggen_map_selection_gradient_20260913.py`
- 검증 기록: `outputs/20260913_ss1_gradient/manifest.json`
- 복원 단계: `outputs/20260913_ss1_gradient/title_restore_steps.png`
- 전체 화면 재구성: `outputs/20260913_ss1_gradient/after_reconstructed.png`

상태를 불러온 직후에는 저장된 예전 VRAM이 표시될 수 있으므로 맵 선택 화면을 나갔다 다시 들어가서 확인한다.
