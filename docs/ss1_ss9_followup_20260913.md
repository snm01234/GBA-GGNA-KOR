# 갱신된 ss1~ss9 추가 교정 (2026-09-13)

사용자 요청에 따라 중단된 작업을 최신 메인 TIP에서 다시 생성했다.

- 기준 TIP: `0c84d052199aaab52c26b052859901dfcc07031f1b3523e968b1387bbc1e4191`
- 후보 SHA-256: `8636d8fe8df37715fa3c1fe518f196ceb722f501102bbf4fded2798c449dfaf9`
- 후보: `outputs/20260913_ss1_ss9_followup/ggen_ss1_ss9_fixed_20260913.gba`
- 검증: `outputs/20260913_ss1_ss9_followup/manifest.json`
- 화면 재구성: `outputs/20260913_ss1_ss9_followup/ss1_ss9_reconstructed.png`

## 수정

1. 도몬 캇슈 이름의 `명경지술`을 `명경지수`로 수정. 도감 본문의 명경지수는 이미 정상이다.
2. 아가마 도감 설명의 `넬 아르가마`를 `넬 아가마`로 수정.
3. `발 바르`를 `발 바로`로 수정. 목록·도감의 이름 소유자 5곳에 반영.
4. 저장 화면 FBtl의 `掃討作戦実行中`을 `소탕 작전 실행 중`으로 번역. 같은 테이블의 `「直前にクリアしたステージ」`도 `직전에 클리어한 스테이지`로 번역하고 괄호를 보존. 12×12 원본 글리프를 직접 확인해 `ニューヤーク廃墟`를 `뉴야크 폐허`로 번역하고 FCE2D8/FCE1A0 양쪽에 반영.
5. 분해실행·강화실행·보급실행·처분실행 및 동일 계열 개조실행의 포커스 이미지: 현재 캔슬 배경에서 한글 잉크·테두리를 제거하여 배경 틀을 추출한 후 한글을 다시 그림. 이전 일본어 글자 제거 과정의 선·음영 잔상을 제거했다. 리소스 주소는 유지하고 전용 타일과 조회 항목만 추가했다.
6. P 디펜서와 같은 폭의 설명란에서 사용하는 `모든 사격 공격을 막는다` 8건을 `모든 사격을 막는다`로 변경. 폭은 104px에서 80px로 줄어 88px 표시 영역에 들어간다.

번역 레코드 18건, 텍스트 소유자 19곳, 실행 포커스 이미지 5종을 수정했다. 최신 TIP과의 변경 바이트는 3,613개이며 허용 범위 밖 변경은 없다.

## 검증 범위

- ss1/2/3/4/9 수정 전 글리프 마스크가 캡처된 VRAM의 해당 문구와 정확히 일치했다. ss9는 잘린 기존 문구의 표시 영역 88px를 대조했다.
- ss5~8의 실행 버튼 OBJ 타일 8개 묶음을 기존 리소스와 바이트 단위로 대조하고 수정 리소스로 재구성했다.
- 수정한 모든 한글 토큰을 실제 ROM 폰트 글리프와 대조했다.
- 다시 읽은 OBJ 리소스가 배경 틀 + 한글 마스크와 정확히 일치한다.
- 일반 버튼·캔슬 등 수정하지 않은 27개 이미지와 팔레트는 동일하다.
- 원본 ss1~ss9와 배터리 세이브는 수정하지 않는다.
- PNG 결과는 ROM/VRAM 기반의 결정적 화면 재구성이며 에뮬레이터 실시간 재생 캡처는 아니다. 에뮬레이터 실행 검증은 수행하지 않았다.

저장 상태에는 예전 VRAM이 들어 있으므로 새 ROM으로 상태를 불러온 뒤 해당 메뉴를 나갔다 다시 들어가야 수정 리소스가 다시 로드된다.

## 재현 도구

- `tools/inspect_ggen_ss1_ss9_20260912.py`: 최신 ROM의 실제 한글 글리프 매핑과 캔슬 배경 추출.
- `tools/patch_ggen_ss1_ss9_20260912.py`: 기준 TIP SHA와 빈 공간을 검증하고 후보 생성.
- `tools/verify_ggen_ss1_ss9_20260913.py`: 리소스/폰트/VRAM 대조 및 화면 재구성.
- 원장 스냅샷: `analysis/ggen_advance_translation_merged_20260913_ss1_ss9.json`.

작업 중 추가 승격을 덮어쓰지 않도록 승격 직전에도 기준 TIP SHA를 다시 확인한다.

## 승격 결과

고정 메인 파일 `SD Gundam GGeneration Advance (Korean).gba`에 승격 완료. 직전 TIP 백업은 `integrated/main_tip/backups/20260912T192915Z_user_requested_ss1_ss9_latest_tip_followup_20260913/`에 보관했다.
